{
	"private": true,
	"name": "bottles",
	"version": "0.1.0",

	"scripts": {
		"clean": "run-p clean:*",
		"build": "run-p build:*",

		"clean:templates": "rimraf ./dist/templates.json",
		"prebuild:templates": "run-p clean:templates",
		"build:templates": "ts-node ./scripts/build/templates",
		"build:containers": "sh ./scripts/build/docker.sh"
	},

	"devDependencies": {
		"@types/node": "^12.7.8",
		"fast-glob": "^3.0.4",
		"npm-run-all": "^4.1.5",
		"rimraf": "^3.0.0",
		"ts-node": "^8.4.1",
		"typescript": "^3.6.3"
	}
}
